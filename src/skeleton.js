const CustomSkeleton = () => {
    return (
        <div className = "skeleton" style = {{
            height: "2rem",
            width: "100%"
        }}>

        </div>
    );
}

export default CustomSkeleton;